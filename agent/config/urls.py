"""
URL configuration for agent project.
"""
from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
    # Add more endpoints here if needed later
]
